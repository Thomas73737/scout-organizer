kind = "web"
previewPath = "/"
title = "San George Scouts"
version = "1.0.0"
id = "artifacts/scouts-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18199

[services.development]
run = "pnpm --filter @workspace/scouts-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/scouts-web", "run", "build" ]
publicDir = "artifacts/scouts-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18199"
BASE_PATH = "/"
